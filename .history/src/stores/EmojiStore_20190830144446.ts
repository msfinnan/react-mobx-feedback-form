import { observable, action } from "mobx"

export class EmojiStore {
    // @observable public buttonClicked: boolean;

    // @observable public emojiButtonClicked: string;

    // @action
    // public onButtonClick = (): void => {
    //     this.buttonClicked = !this.buttonClicked;
    //     this.emojiButtonClicked = "new emoji";
    //     // console.log(`src of emoji is ${this.props.src}`);
    //     console.log("buttonClicked value is " + this.emojiButtonClicked);
    // }
}

